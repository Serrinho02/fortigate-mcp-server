"""
Pure, vendor-agnostic logic operating on plain dicts / canonical models --
no I/O, no adapter or repository imports. Phase 3 scope: `diff.py` only;
analysis engines (duplicate/shadowed/any-any detection, etc.) are Phase 4.
"""
